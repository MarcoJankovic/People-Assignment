@model List<Person>

@if(Model.Count == 0)
{
    <div class="bg-success text-danger text-center">you an not find </div>
}
else
{
    @foreach (Person person in Model)
    {
        <partial name="_PersonRow" model="person" />
    }
}

